import 'package:flutter/material.dart';

class AdminPickupTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('PickupTab'),
    );
  }
}
